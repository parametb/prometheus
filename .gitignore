# ── Node modules ──
node_modules/
package-lock.json

# ── API Keys — ห้าม commit เด็ดขาด ──
.env
.env.local
.env.*

# ── Python cache ──
__pycache__/
*.py[cod]
*.pyo

# ── macOS ──
.DS_Store

# ── Backup / temp files ──
*.bak
*.tmp
/data/*/history/
